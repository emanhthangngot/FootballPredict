# Sample Data

Small local payloads used to align contracts before real providers and databases are connected.

Current foundation:

- `redis/feature_match_123.json` shows the online feature payload shape for one match.
