# Contracts

Contract-first files shared by backend, AI engine, and mobile.

Current foundation:

- `openapi/footballpredict.v1.yaml` documents the first public API route.
- `internal-ai/predict.schema.json` documents the internal AI prediction request.
- `internal-ai/predict.response.schema.json` documents the internal AI prediction response.
