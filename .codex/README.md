# .codex/

Local-only config, gitignored.

## Working with specs/

1. Read `specs/<NNN>-<short>/spec.md` (canonical).
2. Read `specs/<NNN>-<short>/roles/<role>.md` (match your role).
3. Follow "Definition of done" in role file.
4. Open PR into `dev`.
